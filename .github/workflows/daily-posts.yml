name: Daily Auto Posts (Disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Autopost disabled
        run: echo "Daily automatic posting is disabled."
